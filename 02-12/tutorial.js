// Array methods

/* let words = ["b", "C", "a"];
let nums = [60, 9, 3, 4, 5, 1];
console.log(words.sort((a, b) => a.localeCompare(b)));
console.log(nums.sort((a, b) => b - a)); */
// let res = nums.reduce((value, cv, ci, array) => value + cv, 0);
// console.log(res);
// console.log(nums.findIndex((item, index, array) => item >= 5));
// console.log(nums.findLastIndex((item, index, array) => item >= 5));
// console.log(nums.find((item, index, array) => item < 5));

// let n = [1, 2, 3, 4, 5, 6];
// console.log(n.filter((item, index, array) => item % 2 === 0));
/* console.log(n.every((item, index, array) => item % 2 === 0)); // &&
console.log(n.some((item, index, array) => item % 2 === 0)); // || */

/* let res = n.forEach((item, index, array)=>{
    console.log(item);
}) */
/* let res = n.map((item, index, array)=>{
    console.log(item);
    return item ** 2
})
console.log(res);
console.log(n); */

/* let n = [1, [3], 5, 7]; */
// console.log(n.fill('Asilbek', 2, 5));
// console.log(n.copyWithin(0, 3, 5));
// console.log(n);
// console.log(n.flat(Infinity));
/* let res = n.flatMap((item, index, array) => [item, item + 1]);
console.log(res); */

// console.log(Array.from([1, 2, 3], (item) => item + "hey"));

// let n = [1, 2, 2, 3, 4, 3, 3, 5];
// console.log(Array.from(new Set(n)));

/* let obj = {
    name: 'Asilbek',
    age: 20
}
let arr = Object.entries(obj);
let mapObject = new Map(arr);

console.log(obj);
console.log(arr);
console.log(mapObject); // map Object
console.log(Object.fromEntries(mapObject)); // convert to simple object */



